document.addEventListener('DOMContentLoaded',()=>{const toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('.mobile-nav');if(toggle&&nav)toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.hidden=open});const thumbs=[...document.querySelectorAll('[data-thumb]')];const showImage=btn=>{const id=btn.dataset.thumb,image=document.querySelector('[data-product-main] img'),source=btn.querySelector('img');if(image&&source){image.src=source.src.replace(/width=\\d+/,'width=1600');image.dataset.productImage=id}};thumbs.forEach(btn=>btn.addEventListener('click',()=>showImage(btn)));const form=document.querySelector('[data-product-form]'),select=document.querySelector('[data-variant-select]');if(form&&select){form.querySelectorAll('input[type=radio]').forEach(input=>input.addEventListener('change',()=>{const values=[...form.querySelectorAll('input[type=radio]:checked')].map(x=>x.value);const match=[...select.options].find(o=>values.every(v=>o.textContent.includes(v)));if(match){select.value=match.value;select.dispatchEvent(new Event('change'))}}));select.addEventListener('change',()=>{const id=select.selectedOptions[0]?.dataset.imageId;if(id){const thumb=thumbs.find(btn=>btn.dataset.thumb===id);if(thumb)showImage(thumb)}})}});